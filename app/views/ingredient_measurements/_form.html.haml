= form_for @ingredient_measurement do |f|
  - if @ingredient_measurement.errors.any?
    #error_explanation
      %h2= "#{pluralize(@ingredient_measurement.errors.count, "error")} prohibited this ingredient_measurement from being saved:"
      %ul
        - @ingredient_measurement.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
