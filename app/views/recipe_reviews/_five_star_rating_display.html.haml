- if recipe.average_rating
  - recipe.average_rating.times do
    %span.i.fa.fa-star.fa-2x.yellow-text
  %p.small-text
    = "Average Rating: #{recipe.average_rating} over:"
    = pluralize(recipe.recipe_reviews.count,'rating')
- else
  %p.small-text
    = "No Ratings yet"
